import FeaturePlaceholder from '@/components/ui/FeaturePlaceholder'

export default function ReturnsPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Returns"
      title="Returns workspace scaffolded."
      body="This domain is reserved for return authorization, reverse logistics, and stock disposition handling."
    />
  )
}
